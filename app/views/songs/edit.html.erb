<h1> Song Form </h1>

<%= form_for @song do |f| %>
  <%= f.label 'Name' %>
  <%= f.text_field :name %>

  <%= f.label 'Artist' %>
  <%= f.number_field :artist_id %>

  <%= f.label 'Genre' %>
  <%= f.number_field :genre_id %>

  <%= f.submit "Update Song" %>

<% end %>
